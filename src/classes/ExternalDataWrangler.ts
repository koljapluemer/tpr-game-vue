// making a FieldGrid from a dict is the responsibility of FieldGrid, and so on
// this class is for stuff like actually loading JSON from file, and generating a bunch of fields, mediating, etc.
// also, holding (or passing) the *list* of *all* the possible items, that kind of stuff
export class ExternalDataWrangler {

}